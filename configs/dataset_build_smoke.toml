[paths]
trajectory_manifest = "outputs/smoke/trajectories/trajectory_manifest.json"
semantic_pointcloud_dir = "outputs/smoke/semantic_pointcloud"
semantic_pointcloud_filename = "pointcloud.ply"
semantic_pointcloud_report_filename = "pointcloud_report.json"
work_dir = "outputs/smoke/dataset_build"
dataset_root = "data/target"

[trajectory_to_camera]
camera_pose_resource = "src/camera_resource/camera_pose_presets.yaml#presets.current_forward"

[rendering]
camera_batch_size = 8

[dataset]
group_dir = "robotnav"
scene_dir = "scene-smoke-test"
overwrite = false
